// Development environment.
// `ng serve` runs the app on http://localhost:4200 and talks to the
// Spring Boot backend directly on http://localhost:8080.
export const environment = {
  production: false,
  apiUrl: 'http://localhost:8080'
};
